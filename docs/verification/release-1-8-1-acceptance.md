# ArchSight Solver v1.8.1 发布验收记录

> 状态：发布候选就绪
> 目标版本：`v1.8.1`
> 验收原则：复选框只记录已有可追溯证据的事实；任何未完成项保持未勾选。

## Gate A：版本边界与兼容

- [x] `initial_stress_v1` 保持 v1.8.0 数值兼容，旧请求的算法迁移规则有回归测试。
- [x] `corotational_newton_v1` 的适用范围、非目标和稳定拒绝矩阵进入 API、UI、计算书与可信包。
- [x] 所有结果和失败材料记录算法 ID、契约版本、求解参数、来源与限制。

## Gate B：共回转单元与平衡方程

- [x] 当前坐标、当前弦长和刚体转角真实进入基本变形、内力与一致切线。
- [x] 刚体平移/转动客观性、小位移线性极限和解析切线有限差分复核通过。
- [x] 压力软化、拉力加刚、端部释放、节点弹簧和一般约束的符号与平衡通过定向测试。
- [x] 支持的荷载与初始应变进入同一残差；未支持能力稳定拒绝且无静默降级。

## Gate C：路径、收敛、稳定与失败恢复

- [x] 全 Newton、确定性线搜索、自适应增步和 cutback 按版本化参数运行。
- [x] 相对/绝对残差、相对/绝对位移增量和能量判据同时可审查。
- [x] 平衡状态与稳定状态独立报告；临界点附近不能以“已收敛”替代稳定判断。
- [x] cutback 用尽、切线奇异或稳定越界时保留最后收敛点、全部尝试记录和稳定错误码。
- [x] 工况、组合、固定预荷载和变量荷载路径分别求解，不对非线性结果做线性叠加。

## Gate D：初始缺陷与方法比较

- [x] 显式节点缺陷及屈曲模态缺陷进入规范化模型、当前几何、哈希和导出证据。
- [x] 缺陷幅值、模态号、归一化和来源可回放；未选择缺陷时不暗中注入默认偏心。
- [x] `MethodComparison@1` 只比较可比指标，并记录首阶、旧算法、新算法及屈曲参考的来源哈希和不可比原因。

## Gate E：计算过程、教学表达与图形

- [x] `NonlinearPathTrace@1` 包含步、迭代、残差、位移/能量增量、线搜索、cutback、稳定指标和有界关键帧。
- [x] 过程播放使用 canonical trace，不保存每次迭代的整份模型副本。
- [x] 路径图完整检测最后收敛、cutback、残差峰值、最小稳定指标和终止点；图面标签可避碰，完整数值不得丢失。
- [x] 入门/工程/算法三层解释绑定算法 ID；不把线性屈曲因子写成安全系数，不把放大模态写成真实变形。
- [x] 梁系与桁架不出现伪造的非线性播放或方法比较入口。
- [x] 至少两组公开教学算例可一键打开、复算、播放并下载证据。

## Gate F：计算书与可信计算包

- [x] 屏幕、标准/详细 DOCX、XLSX 与可信计算包对算法、路径、关键点、最后收敛点和稳定诊断同源。
- [x] 详细计算书包含代表性共回转变换、残差方程、收敛表、稳定性分离和方法比较；标准计算书保留可复核摘要。
- [x] 失败审查材料不伪造最终位移或内力，并能恢复最后收敛状态及失败尝试。
- [x] 契约有 JSON Schema、规范化、哈希、体积上限和旧包兼容测试。

## Gate G：公开基准与回归

- [x] 单元客观性、切线复核、线性极限、拉压方向和受压悬臂基准通过。
- [x] Williams toggle frame、Lee frame 极限点前路径和初始缺陷柱基准通过。
- [x] 端释放、弹簧、斜支座、分布/构件集中/温度荷载及支座位移逐类通过或稳定拒绝测试。
- [x] 全部 71 个公开 benchmark、独立刚度法、P-Delta v1 和线性屈曲回归通过。
- [x] 相同请求的结果、路径、关键点和失败码确定性通过。

## Gate H：产品、浏览器与发布制品

- [x] 前端单测、lint、类型检查和生产构建通过。
- [x] Chromium 功能闭环及 Chromium/Firefox/WebKit 导出矩阵通过。
- [x] 后端全量测试、API/OpenAPI、CLI/MCP/Host 兼容和 Docker 冒烟通过。
- [x] `aios-structural`、`aios-arch`、`aios-ceo` 复核 P0/P1 为 0。
- [x] 版本文件、CHANGELOG、README、Roadmap、SBOM、安全扫描、镜像、回滚和发布说明证据齐备。
- [x] `git diff --check` 通过，提交符合 Lore Commit Protocol，工作树不含无关改动。

## 当前证据

- 2026-08-24：v1.8.0 稳定分析定向回归 29 项通过；确认旧算法为固定初始几何的初始应力迭代，作为 `initial_stress_v1` 兼容基线保留。
- 2026-08-24：完美柱在临界荷载以上仍可求得平衡，证明 v1.8.1 必须分离 `equilibriumStatus` 与 `stabilityStatus`，不能把 Newton 收敛解释为安全。
- 2026-08-24：后端全量 `736 passed, 2 skipped`；前端单元测试 `444/444`，lint、TypeScript、生产构建和生产依赖审计通过。
- 2026-08-24：公开 benchmark `71/71` 通过，其中新增 `GNA-001` 至 `GNA-005`；四个公开学习工程覆盖梁、桁架、线性框架和 GNA/GNIA。
- 2026-08-24：本地正确运行环境下 Chromium 发布回归 `51/51`；Chromium、Firefox、WebKit DOCX 同源图形与数据曲线导出矩阵 `12/12`；稳定与关键点三浏览器矩阵 `15/15`。
- 2026-08-24：候选镜像 `archsight-solver:v1.8.1-candidate`（`sha256:7ba6b3527b74aa4d0e2c9caa555e1d4071b22097febf909a8b2914e43bff73f6`）健康运行，根页面返回 HTTP 200；真实 GNA 教学链路 `2/2`、镜像 Host 集成 `1/1` 通过。
- 2026-08-24：wheel、源码包和 Host Client 候选制品完成隔离安装与契约验证；SPDX SBOM 记录 109 个包、2959 个文件。
- 2026-08-24：Trivy 原始扫描包含 14 个 HIGH、3 个 CRITICAL，均未提供 `FixedVersion`；发布门禁要求的“已有修复版本但未升级”的 HIGH/CRITICAL 为 0，继续作为上游待修风险跟踪，不将原始发现误写为零漏洞。
- 2026-08-24：`aios-structural`、`aios-arch`、`aios-ceo` 独立复核均为 P0=0、P1=0；P2 不扩张为本版发布阻断。
- 2026-08-24：本记录只证明源码与本地制品达到发布候选状态；尚未创建 `v1.8.1` tag，未推送镜像仓库，未更新线上。线上仍保持 `v1.8.0`。
